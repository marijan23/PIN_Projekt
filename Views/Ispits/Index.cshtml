@model IEnumerable<PIN_Projekt.Models.Ispit>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Ocijena)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BrojBodova)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DatumPolaganja)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StudentParent)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PredmetParent)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Ocijena)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BrojBodova)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DatumPolaganja)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StudentParent.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PredmetParent.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
